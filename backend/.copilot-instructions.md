/\*\*

- ✅ Copilot Coding Standards (Clean Code - Module 5.x AI Scheduler):
-
- 📏 Line Rules:
- - Keep code files under 250 lines if possible (allow 270–300 when justified).
- - Never delete code or comments to meet limits. Instead, modularize.
- - Split large functions into smaller named helpers (max 50–75 lines per func).
- - Test files can exceed limits (up to 400 lines) for completeness.
-
- 🔧 Quality Rules:
- - No `any` in production; use explicit types or generics.
- - Favor descriptive, typed parameters and return types.
- - Avoid nested blocks deeper than 4 levels.
- - Complexity limit: 12 for production code, 15 for tests.
-
- ⚙️ Functional Requirements:
- - Maintain O(V + E) for all graph-based algorithms.
- - Use epsilon comparison for floats (ε = 0.001).
- - Separate compute, validate, and assign responsibilities.
-
- 📦 Folder Structure:
- - Split utilities, validators, calculators, analyzers, and orchestrators into separate files.
- - Centralize float calculation logic in `FloatCalculator.ts`.
- - Centralize critical path analysis in `CriticalPathAnalyzer.ts`.
- - Centralize flag assignment logic in `TaskFlagAssigner.ts`.
- - Use `types/` directory for shared interfaces.
- - Use `utils/` directory for helper functions.
-
- 🎯 Module 5.4 Specific Requirements:
- - Float calculations must use epsilon-based precision (DEFAULT_EPSILON = 0.001).
- - Critical path analysis must identify all paths with zero total float.
- - Task flag assignment must support multiple flag types per task.
- - Batch processing must handle large datasets (5000+ tasks) efficiently.
- - Error handling must be graceful and provide meaningful messages.
- - Performance benchmarks: <10ms for 5000 tasks, <50MB memory for 1K tasks.
-
- 🧪 Testing Standards:
- - Comprehensive test coverage with edge cases, boundary conditions, and stress tests.
- - Performance benchmarks included in test suites.
- - Error resilience testing with corrupted/invalid data.
- - Memory usage validation during large batch operations.
- - Test functions may exceed 75 lines for comprehensive scenario coverage.
-
- 🚀 Refactoring Guidelines:
- - When a method exceeds 50 lines, extract helper methods with descriptive names.
- - When a file exceeds 250 lines, consider splitting into:
- - Core functionality (main class)
- - Helper utilities (utils/ folder)
- - Type definitions (types/ folder)
- - Validation logic (separate validator class)
- - Prefer composition over inheritance.
- - Use dependency injection for testability.
-
- 📋 Example Modularization Patterns:
- ```typescript

  ```
- // Instead of one large service:
- class Module54Service {
- // 300+ lines with mixed responsibilities
- }
-
- // Prefer modular approach:
- class Module54Service {
- constructor(
-     private floatCalculator: FloatCalculator,
-     private pathAnalyzer: CriticalPathAnalyzer,
-     private flagAssigner: TaskFlagAssigner
- ) {}
- // Core orchestration only
- }
-
- class FloatCalculationCore {
- // Pure calculation logic
- }
-
- class Module54ResultBuilder {
- // Result object construction
- }
- ```
  */
  ```
