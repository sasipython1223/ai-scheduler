/// Use Uncle Bob’s Clean Code rules across all code:
/// - Meaningful names (no abbreviations)
/// - Small, single-responsibility functions
/// - Flat code structure (early returns)
/// - Well-structured services, avoid logic in routes/components
/// - Add TS interfaces and avoid `any`
/// - Follow folder-by-feature pattern
